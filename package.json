{
  "name": "webhooks-v2",
  "version": "1.0.0",
  "description": "A simple webhook control panel ",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "npm run build && npm start",
    "build": "echo Building... && npx tsc && echo Build Finished",
    "setup": "echo Setup started... && npm run build && npm uninstall typescript && npm uninstall @types/node-fetch && npm prune && echo Setup complete, you may now run 'npm start' "
  },
  "author": "Jason Mayer",
  "license": "ISC",
  "dependencies": {
    "@types/node-fetch": "^2.5.10",
    "node-fetch": "^2.6.1",
    "prompt-sync": "^4.2.0",
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "@types/node": "^14.14.41"
  }
}
